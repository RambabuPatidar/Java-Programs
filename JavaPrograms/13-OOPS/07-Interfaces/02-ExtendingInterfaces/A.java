package com.OOPS.Interfaces.ExtendingInterfaces;
// we cannot extend an interface to a class. obviously make sense because interfaces have abstract
//methods and classes have concrete methods so interface will not allow us to have concrete classes
//in it
// so below is illegal.

// public interface A extends InterfaceToClass {
//    void fun();
//}

public interface A {
    void fun();
}
